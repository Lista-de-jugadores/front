// Importa las action types acá

const initialState = {
  products: [],
  productDetail: {},
};

const rootReducer = (state = initialState, action) => {
  switch (action.type) {
   
      };
   
}
  export default rootReducer;
